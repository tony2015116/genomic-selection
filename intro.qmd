---
jupyter: python3
---

# Introduction

This is a book created from markdown and executable code.

See @knuth84 for additional discussion of literate programming.

```{python}
1 + 1
```

For a demonstration of a line plot on a polar axis, see @fig-polar.

```{python}
#| label: fig-polar
#| fig-cap: "A line plot on a polar axis"

import numpy as np
import matplotlib.pyplot as plt

r = np.arange(0, 2, 0.01)
theta = 2 * np.pi * r
fig, ax = plt.subplots(
  subplot_kw = {'projection': 'polar'} 
)
ax.plot(theta, r)
ax.set_rticks([0.5, 1, 1.5, 2])
ax.grid(True)
plt.show()
```

😄

```{r}
library(dplyr)
iris |>
  head()
```

你好，欢迎来到这里，法拉士大夫艰苦看到房价肯定舒服就看了看地方附近了多少开发艰苦撒旦发喀什酱豆腐考虑的就是开发的首款了房间肯定是积分考虑到实际付款了多少防空雷达技术风口浪尖的是开了房间考了多少分考多少分考多少分考了多少分艰苦拉萨的飞机肯定是法艰苦撒旦解放考了多少分了肯定是防空雷达数据防空雷达数据付款了法律快速反击克里斯蒂反抗类毒素分类。
